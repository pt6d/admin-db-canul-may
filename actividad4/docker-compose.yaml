services:

  mysql_db:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: practica4_mysql

    environment:
      MYSQL_ROOT_PASSWORD: secret
      MYSQL_DATABASE: practica4

    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data: